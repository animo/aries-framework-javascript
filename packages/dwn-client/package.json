{
  "name": "@aries-framework/dwn-client",
  "main": "build/index",
  "types": "build/index",
  "version": "0.4.2",
  "files": [
    "build"
  ],
  "license": "Apache-2.0",
  "publishConfig": {
    "access": "public"
  },
  "homepage": "https://github.com/hyperledger/aries-framework-javascript/tree/main/packages/dwn-client",
  "repository": {
    "type": "git",
    "url": "https://github.com/hyperledger/aries-framework-javascript",
    "directory": "packages/dwn-client"
  },
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf ./build",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "jest --forceExit --detectOpenHandles"
  },
  "dependencies": {
    "@aries-framework/askar": "^0.4.2",
    "@aries-framework/core": "0.4.2"
  },
  "devDependencies": {
    "@aries-framework/node": "^0.4.2",
    "@hyperledger/aries-askar-nodejs": "^0.2.0-dev.1",
    "nock": "^13.3.0",
    "rimraf": "^4.4.0",
    "typescript": "~4.9.5"
  }
}
